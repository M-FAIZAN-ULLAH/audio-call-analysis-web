# Flask and Flask extensions
Flask==3.1.2
Flask-Cors==5.0.0

# Hume AI SDK
# Note: Using version 0.6.0 for HumeBatchClient and ProsodyConfig compatibility
# If migrating to new SDK, use: hume>=0.13.0
hume==0.6.0

# Flask dependencies (usually auto-installed, but included for completeness)
Werkzeug==3.1.2
ItsDangerous==2.2.0
Blinker==1.9.0
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.3.1

# Standard library modules used (no installation needed):
# - json
# - logging
# - datetime
# - os
# - time
# - re
